<html>
  <head>
    <title>Restaurant Sample App</title>
    <style>
      p, h1, h2, h3, h4, ul, li, a, label {
        font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
        color: #157DA6;
        text-align: center;
      }
      .centered {
        text-align: center;
      }
    </style>
  </head>
  <body>
    <h1></h1>
    <h3>Edit a restaurant!</h3>
      <%# a form that edits an existing restaurant %>
    <form action='/restaurants/<%= restaurant.id %>' method='POST' class='centered'>
      <label>name</label>
      <input type='text' name='name' required value='<%= restaurant.name %>' />
      <label>location</label>
      <input type='text' name='location' value='<%= restaurant.location %>' required/>
      <input type='hidden' name='_method' value='PUT'>
      <input type='submit' value='update restaurant' />
    </form>
    <h4><a href='/'>All Restaurants</a></h4>

  </body>
</html>
